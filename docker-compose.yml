version: "2"
services:
        nodeserver:
                build:
                        context: ./app
                ports:
                        - "5000:5000"
                image: docker.io/somnathdaem/nodeapp
        nginix:
                restart: always
                build:
                        context: ./nginx
                ports:
                        - "80:80"
                        - "443:443"
                image: docker.io/somnathdaem/nginxapp
